# Average of Two Inputs

This project reads two numbers using a reusable method and calculates their average, demonstrating method reuse in C#.

## What I Learned
- How to create a reusable method that returns a value
- How to call the same method multiple times
- How methods with return types work in C#
- How to calculate average using two method return values
